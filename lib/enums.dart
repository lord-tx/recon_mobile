/// The Type of Item being processed by the ChatBubble
enum ChatItemType{
  image,
  text,
  emoji,
  mixed,
}

/// The Type of Item being veiwed by the user and processed by the bubble.
enum ChatType{
  received,
  sent,
}
